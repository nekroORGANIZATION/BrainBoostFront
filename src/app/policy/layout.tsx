export const metadata = {
  title: 'Політика конфіденційності — BrainBoost',
  description: '...',
};

export default function PrivacyLayout({ children }: {children: React.ReactNode}) {
  return <>{children}</>;
}